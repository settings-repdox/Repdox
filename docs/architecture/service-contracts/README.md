# Service Contracts

Service contract definitions (interfaces, DTOs, public API surfaces) should be defined here and versioned.

Domains publish public contracts that production and other domains consume.
