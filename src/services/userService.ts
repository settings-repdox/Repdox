// Service scaffold for user-related application behavior.

import type { UserId } from "../shared/types";

export class UserService {
  async getUserProfile(userId: UserId) {
    // Placeholder for future user profile orchestration.
    return null;
  }
}
