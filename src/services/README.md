# Service Layer

This folder will contain service modules that encapsulate application business logic and orchestration.

Keep existing `src/lib/` intact during Phase 1 and use this folder for new service scaffolding.
