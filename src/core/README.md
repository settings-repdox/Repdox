# Core

Core contains true cross-cutting capabilities used across the repository. Core must not own business domains such as `gaming` or `broadcast`.

Examples:

- auth
- users
- permissions
- branding
- notifications
- analytics
- core/services
